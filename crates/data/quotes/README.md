# Talk entries

Each `.ron` file contains a list of `QuoteDefinition` entries. The loader embeds
and validates all files. Use unique IDs and original named dialogue. Empty state
tags mean any party state; list all four seasons for year-round dialogue.
